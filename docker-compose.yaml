version: "3"

services:
  php:
    container_name: deserve_php
    build: ./.docker/php-fpm
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - 9000:9000

  queue:
    container_name: deserve_queue
    build: ./.docker/php-fpm
    volumes:
      - .:/app
    working_dir: /app
    depends_on:
      - mariadb
    command: sh -c "php artisan queue:work"

  nginx:
    container_name: deserve_nginx
    image: nginx:alpine
    volumes:
      - .:/app:ro
    working_dir: /app
    ports:
      - 80:80
      - 443:443
    depends_on:
      - php

  composer:
    container_name: deserve_composer
    image: composer:latest
    restart: "no"
    volumes:
      - .:/app
    environment:
      TERM: "xterm-256color"

  mariadb:
    container_name: deserve_mariadb
    image: mariadb:latest
    volumes:
      - ./.data/mariadb:/var/lib/mysql
    environment:
      MARIADB_USER: ${DB_USERNAME}
      MARIADB_ROOT_PASSWORD: ${DB_PASSWORD}
      MARIADB_ALLOW_EMPTY_ROOT_PASSWORD: "yes"
      MARIADB_DATABASE: ${DB_DATABASE}
    ports:
      - 3306:3306

  nodejs:
    container_name: deserve_nodejs
    image: node:17-alpine3.15
    volumes:
      - .:/usr/app
    working_dir: /usr/app
    ports:
      - 3000:3000
    environment:
      NODE_OPTIONS: --openssl-legacy-provider
